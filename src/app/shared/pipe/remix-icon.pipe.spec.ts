import { RemixIconPipe } from './remix-icon.pipe';

describe('RemixIconPipe', () => {
  it('create an instance', () => {
    const pipe = new RemixIconPipe();
    expect(pipe).toBeTruthy();
  });
});
